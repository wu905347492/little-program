import _ from '../vendor/lodash.core';

export default {
  assignIn: _.assignIn,
  before: _.before,
  bind: _.bind,
  chain: _.chain,
  clone: _.clone,
  compact: _.compact,
  concat: _.concat,
  create: _.create,
  defaults: _.defaults,
  defer: _.defer,
  delay: _.delay,
  each: _.each,
  escape: _.escape,
  every: _.every,
  filter: _.filter,
  find: _.find,
  flatten: _.flatten,
  flattenDeep: _.flattenDeep,
  forEach: _.forEach,
  has: _.has,
  head: _.head,
  identity: _.identity,
  indexOf: _.indexOf,
  isArguments: _.isArguments,
  isArray: _.isArray,
  isBoolean: _.isBoolean,
  isDate: _.isDate,
  isEmpty: _.isEmpty,
  isEqual: _.isEqual,
  isFinite: _.isFinite,
  isFunction: _.isFunction,
  isNaN: _.isNaN,
  isNull: _.isNull,
  isNumber: _.isNumber,
  isObject: _.isObject,
  isRegExp: _.isRegExp,
  isString: _.isString,
  isUndefined: _.isUndefined,
  iteratee: _.iteratee,
  keys: _.keys,
  last: _.last,
  map: _.map,
  matches: _.matches,
  max: _.max,
  min: _.min,
  mixin: _.mixin,
  negate: _.negate,
  noConflict: _.noConflict,
  noop: _.noop,
  once: _.once,
  pick: _.pick,
  reduce: _.reduce,
  result: _.result,
  size: _.size,
  slice: _.slice,
  some: _.some,
  sortBy: _.sortBy,
  tap: _.tap,
  thru: _.thru,
  toArray: _.toArray,
  uniqueId: _.uniqueId,
  values: _.values
};
